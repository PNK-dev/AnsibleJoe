<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Información del Proyecto SO</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .info { background: #f0f0f0; padding: 20px; border-radius: 8px; margin: 20px 0; }
    </style>
</head>
<body>
    <h1>📊 Información Detallada del Proyecto SO</h1>
    
    <div class="info">
        <h2>Sistema</h2>
        <p><strong>Hostname:</strong> {{ ansible_hostname }}</p>
        <p><strong>OS:</strong> {{ ansible_distribution }} {{ ansible_distribution_version }}</p>
        <p><strong>Kernel:</strong> {{ ansible_kernel }}</p>
        <p><strong>Arquitectura:</strong> {{ ansible_architecture }}</p>
    </div>

    <div class="info">
        <h2>Red</h2>
        <p><strong>IP:</strong> {{ ansible_default_ipv4.address }}</p>
        <p><strong>Interfaz:</strong> {{ ansible_default_ipv4.interface }}</p>
        <p><strong>Dominio:</strong> {{ network_domain | default('proyecto-so.local') }}</p>
    </div>

    <div class="info">
        <h2>Servicios Configurados</h2>
        <ul>
            <li>🌐 HTTP/Apache - Puerto 80</li>
            <li>📁 FTP/vsftpd - Puerto 21</li>
            <li>🔍 DNS/BIND - Puerto 53</li>
            <li>🛡️ Firewall + Suricata IDS/IPS</li>
            <li>🌐 IPv6 (radvd + DHCPv6)</li>
        </ul>
    </div>

    <p><a href="/">← Volver al inicio</a></p>
</body>
</html>